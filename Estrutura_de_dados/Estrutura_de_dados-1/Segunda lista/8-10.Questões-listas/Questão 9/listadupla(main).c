
Lista *lista;

lista = cria_lista();

libera_lista(lista);

int x = tamanho lista(lista);

if(lista_cheia(lista))

int x = lista_vazia(Lista* lista);

if(lista_vazia(lista))

int x = insere_lista_inicio (lista, dados_cidade);

int x = insere_lista_final (lista, dados_cidade);

int x =  insere_lista_ordenada(Lista* lista, struct cidade c1);
